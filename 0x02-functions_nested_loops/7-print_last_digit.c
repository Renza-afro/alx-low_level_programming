#include "main.h"

/**
* print_last_digit-> print last digit of a number
* @n: input as integer
* Return: last digigt
*/

int print_last_digit(int n)
{

int l;
1 = n % 10;
if (l < 0)
{
_putchar(-l + 48);
return (l);
}
}
