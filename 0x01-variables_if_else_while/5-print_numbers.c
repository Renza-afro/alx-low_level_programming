#include <stdio.h>

/**
* main-> prints all single of base 10 numbers from 0, followed by a new line
* all code in the main
* Return: always 0
*/

int main(void)
{

int baseTen;
for (baseTen = 0; baseTen < 10; baseTen++)
{
printf("%d", baseTen);
}
putchar('\n');

return (0);
}
